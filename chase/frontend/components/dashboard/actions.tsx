import React from "react";
import WidgetTemplate from "./widgettemplate";

export default function ActionsWidget() {
  return (
    <>
      <WidgetTemplate cardTitle="Aktionen">
        This is space for action buttons like message to chair, request to the
        Research Service
      </WidgetTemplate>
    </>
  );
}
