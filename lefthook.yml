pre-commit:
  parallel: true
  commands:
    format:
      run: bunx prettier --write --ignore-unknown "{staged_files}"
      stage_fixed: true
pre-push:
  commands:
    lint:
      run: bunx eslint "{push_files}"
