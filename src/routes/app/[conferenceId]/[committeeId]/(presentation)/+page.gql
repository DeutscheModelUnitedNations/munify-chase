query CommitteePresentationQuery($committeeId: ID!) @blocking {
	findFirstCommittee(where: { id: $committeeId }) {
		id
		abbreviation
		name
		status
		statusHeadline
		statusUntil
		totalPresent
		simpleMajority
		twoThirdsMajority
		paperSupportThreshold
		activeAgendaItem {
			id
			title
		}
		whiteboardContent
	}
}
